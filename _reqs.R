### Require packages that are needed for R Markdown / quarto management
### but are otherwise not formal dependencies that renv sees.

box::use(
	markdown,
	mime,
	yaml,
	rmarkdown,
	lintr,
	devtools,
	usethis
)

# END OF FILE
